server:
  port: 8092
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/med-djamm
    username: postgres
    password: Windev2018
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-description: "init"
    baseline-version: 0
